<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">Questions CRUD</a>
    <button class="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('users_index') }}">
                    Users
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('questions_index')}}">
                    Questions
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('answers_index') }}">
                    Answers
                </a>
            </li>
        </ul>
    </div>
</nav>